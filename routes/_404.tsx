import { define } from '@/lib/utils.ts';

export default define.page(() => (
    <main>
        <div>
            <h1>404</h1>
            <p>Couldn’t find what you’re looking for.</p>
        </div>
    </main>
));
